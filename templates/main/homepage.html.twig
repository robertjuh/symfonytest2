{% extends 'base.html.twig' %}

{% block title %}
    {{parent()}}
    Titeltje

{% endblock %}

{% block body %}


{{countje * 2}}

{% if countje < 999 %}

<h3> countje is {{countje}} <h3>

{% endif %}


<div>
    <table>

        <tr>

            <a href="{{path('app_datapoint_show', {id : randomObj.x })}}">
                 X : {{randomObj.x}}
            </a>


            <td>{{randomObj.y}}</td>



        </tr>
    </table>

</div>

{% endblock %}